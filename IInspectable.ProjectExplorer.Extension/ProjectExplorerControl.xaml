<UserControl x:Class="IInspectable.ProjectExplorer.Extension.ProjectExplorerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vsShell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.14.0"
             xmlns:imaging="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.Imaging"
             xmlns:catalog="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.ImageCatalog"
             xmlns:platformUi="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Imaging"
             xmlns:platformUtils="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Utilities"
             xmlns:ui="clr-namespace:IInspectable.ProjectExplorer.Extension.UI"
             xmlns:vsui="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.14.0"
             Background="{DynamicResource {x:Static vsShell:VsBrushes.ToolWindowBackgroundKey}}"
             Foreground="{DynamicResource {x:Static vsShell:VsBrushes.ToolWindowTextKey}}"
             platformUi:ImageThemingUtilities.ImageBackgroundColor="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBackgroundColorKey}}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Name="MyToolWindow">

    <UserControl.Resources>
        <platformUtils:BrushToColorConverter x:Key="BrushToColorConverter"/>

        <Style x:Key="StatusIndicator" TargetType="Rectangle">
            <Setter Property="Fill" Value="Transparent"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Unavailable">
                    <Setter Property="Fill" Value="Transparent"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Unloaded">
                    <Setter Property="Fill" Value="#1BA1E2"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Loaded">
                    <Setter Property="Fill" Value="#339933"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <DataTemplate x:Key="ProjectItemTemplate" >
            <Border BorderThickness="0"
                    
                    >
                <Grid Margin="0,2">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3" />
                        <ColumnDefinition Width="24" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Rectangle Grid.RowSpan="3" Style="{StaticResource StatusIndicator}"/>

                    <imaging:CrispImage Grid.Column="1" Grid.Row="0"
                                        Margin="3,2" 
                                        Width="16" 
                                        Height="16" 
                                        Moniker ="{x:Static catalog:KnownMonikers.CSProjectNode}"/>

                    <TextBlock Grid.Column="2" Grid.Row="0" Margin="0,3,3,0" Text="{Binding Name}" ToolTip="{Binding Name}"/>


                    <imaging:CrispImage Grid.Column="1" Grid.Row="1"
                                        Margin="3,2" 
                                        Width="16" 
                                        Height="16" 
                                        Moniker ="{x:Static catalog:KnownMonikers.FolderOpened}"/>

                    <TextBlock Grid.Column="2" Grid.Row="1" Margin="0,3,3,0" Text="{Binding Directory}" ToolTip="{Binding Directory}"/>

                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button Content="Click me!" Click="OnButtonClick"/>
        <ui:VsListBox Grid.Row="1" ItemsSource="{Binding}" ItemTemplate="{StaticResource ProjectItemTemplate}"/>
        <TextBlock Grid.Row="2" Text="{Binding Count, StringFormat={}{0} Projects}"/>
    </Grid>

</UserControl>
